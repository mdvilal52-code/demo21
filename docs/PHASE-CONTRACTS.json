{
  "$schema": "./phase-contracts.schema.json",
  "project": "AI Concierge — Luxury Car Rental (Dubai)",
  "version": "1.0.0",
  "updatedAt": "2026-09-26",
  "statusLegend": [
    "PENDING",
    "IN_PROGRESS",
    "FROZEN"
  ],
  "phaseNumbering": "History: phases 1-4 were each scoped and delivered as a single journey step from docs/MASTER-PLAN.md §4. After id 4, this project's git history split into two independently-developed branches, each needing journey-step work that didn't fit any open original-10-phase id: one branch built a real Security Engine and continued the journey (Eligibility/Availability/Alternatives/Quote), appending those at ids 6 and 11-14 rather than renumbering; the other (this branch) built a Gemini-backed conversational reply engine on top of the official WhatsApp channel adapter. Both branches' phase-numbered docs (docs/PHASE-2.md, docs/PHASE-5-CHANNELS.md, docs/PHASE-6.md, docs/PHASE-11.md … docs/PHASE-14.md, docs/phases/PHASE-06.md, …) were written under those branches' own id schemes and were NOT renamed during the 2026-09-25 merge+renumber below — each phase's `phaseDoc` field is the authoritative pointer to its actual detail; the filename's own number is historical, not necessarily this document's current id for that phase. On 2026-09-25 the two branches were merged and, per explicit request, every phase below was renumbered into one clean sequential list (id order = dependency/build order) rather than the prior 'append at the next free integer' scheme — dependsOn was re-derived from the original graph, not fabricated as a flat chain: id 8 (Alternatives) still depends specifically on id 7 (Availability), not on id 6 (Eligibility), because that was true in the original graph regardless of id numbers. Any future work gets the next free sequential id.",
  "rules": {
    "protocol": "docs/PHASE-EXECUTION-PROTOCOL.md",
    "masterPlan": "docs/MASTER-PLAN.md",
    "designSystem": "docs/DESIGN-SYSTEM.md",
    "gateOrder": [
      "typecheck",
      "lint",
      "unit",
      "integration",
      "security",
      "e2e",
      "build",
      "code-review",
      "architecture-review",
      "regression"
    ],
    "freezeRequires": [
      "all deliverables done",
      "all acceptance criteria demonstrated",
      "all gates green locally and in CI",
      "migrations up/down/up clean",
      "no fake success paths (NOT_CONFIGURED used for missing providers)",
      "phase security checklist complete",
      "docs/phases/PHASE-NN.md written",
      "PHASE-CONTRACTS.json updated"
    ]
  },
  "phases": [
    {
      "id": 1,
      "name": "Foundation & Platform Skeleton + Enquiry / Intent Recognition",
      "status": "FROZEN",
      "goal": "Runnable, tested, CI-gated monorepo (api, worker, web, db, cache, observability, security, design tokens) delivering business Step 1 end to end: customer message -> deterministic AI intent recognition -> persisted + audited -> queued for background processing.",
      "deliverables": [
        "pnpm workspaces (no Turborepo — plain tsc per package, pnpm -r respects the dependency graph) + ESLint flat config + Prettier + Vitest + Playwright + .env.example + .nvmrc",
        "apps/api: Fastify 5 + Zod type provider, OpenAPI /docs, /health + /live + /ready, request-id/correlation (AsyncLocalStorage), pino logs, OTel bootstrap (NOT_CONFIGURED by default), env->Zod config, AppError envelope, graceful shutdown, helmet/CORS/rate-limit, POST /v1/enquiries + GET /v1/enquiries/:id",
        "apps/worker: BullMQ worker consuming post-enquiry-processing, idempotent processor + audit event",
        "apps/web: Next.js App Router + Tailwind + original Emerald&Copper components (Monogram/GlassCard/CopperCard/PillButton/StatusChip) + enquiry form + server-side SSRF-safe proxy route",
        "packages/config, packages/domain, packages/ai (RuleBasedIntentEngine + prompt-injection sanitizer), packages/security, packages/observability, packages/contracts, packages/db (Prisma: Tenant/Conversation/Message/IntentRecord/AuditEvent/IdempotencyKey + migration), packages/testing (real-Postgres/Redis test helpers)",
        "docker-compose.yml: postgres, redis (minio/otel-collector/grafana deferred — not needed until a real object-storage or exporter consumer exists)",
        ".github/workflows/ci.yml (full gate pipeline) + security.yml (pnpm audit, gitleaks, semgrep)",
        "docs/ARCHITECTURE.md, docs/phases/PHASE-01.md"
      ],
      "acceptance": [
        "pnpm install && docker compose up -d && pnpm db:migrate && pnpm dev:api / dev:worker / dev:web starts each service",
        "GET /health -> 200 with observability status; GET /docs serves OpenAPI",
        "web enquiry form renders with the design tokens and round-trips a real request through the API",
        "POST /v1/enquiries recognizes intent, persists conversation+message+intent+audit atomically, enqueues background processing; GET /v1/enquiries/:id returns it",
        "all gates pass: typecheck, lint, format, 91 unit + 21 integration + 21 security + 4 e2e (incl. accessibility) tests, production build — see docs/phases/PHASE-01.md §8"
      ],
      "dependsOn": [],
      "phaseDoc": "docs/phases/PHASE-01.md"
    },
    {
      "id": 2,
      "name": "Extract Dates & Location (journey Step 2)",
      "status": "FROZEN",
      "goal": "Take a Phase 1 conversation's validated, intent-recognized message and propose+verify pickup/return dates and pickup/dropoff locations. AI proposes (DateExtractionService, LocationExtractionService); deterministic domain logic verifies (TemporalValidationService) before anything is persisted or returned.",
      "deliverables": [
        "packages/domain: LocationType, AmbiguityCode, ValidationErrorCode, normalizedLocationSchema, dateLocationExtractionResultSchema",
        "packages/ai/step2: DateExtractionService (named-month/range/ISO/numeric/relative/next-weekday dates, ambiguity-safe, DST-aware via Intl), LocationExtractionService (Dubai/UAE gazetteer via a LocationProvider abstraction), TemporalValidationService (PAST_DATE/RETURN_BEFORE_OR_EQUAL_PICKUP/IMPOSSIBLE_DATE/TIMEZONE_MISMATCH/UNSUPPORTED_LOCATION + confidence), DateLocationExtractionOrchestrator",
        "packages/security/resilience.ts: withTimeout, CircuitBreaker, RateLimiter, applied to the location provider seam",
        "packages/db: DateLocationExtraction model + migration, dateLocationExtractionRepository, findLatestMessageForConversation",
        "packages/contracts: extractDatesLocationParamsSchema / extractDatesLocationResponseSchema",
        "apps/api: POST /v1/enquiries/:conversationId/dates-location (dateLocationService.ts + routes/v1/temporal.ts)",
        "docs/PHASE-2.md"
      ],
      "acceptance": [
        "every required test case passes: 15 Oct; 15-19 Oct; tomorrow; next Friday; ambiguous 10/11/26 (never guessed); invalid date; past date; return before pickup; timezone mismatch; Dubai; unknown location; malicious prompt injection",
        "full Phase 1 regression re-run and green (no working Phase 1 functionality broken)",
        "all gates pass: typecheck, lint, format, 180 unit + 32 integration + 29 security tests, 4 e2e, production build — see docs/PHASE-2.md §8"
      ],
      "dependsOn": [
        1
      ],
      "phaseDoc": "docs/PHASE-2.md"
    },
    {
      "id": 3,
      "name": "Determine Vehicle (journey Step 3)",
      "status": "FROZEN",
      "goal": "Map the customer's vehicle preference (exact model, brand only, category only, or a typo) to a real fleet catalog entry, deterministically verified against the tenant's actual inventory — same AI-proposes/domain-verifies split as Steps 1-2, with an explicit third component (VehicleCatalogService) standing for 'database is authoritative, never invent inventory'.",
      "deliverables": [
        "packages/domain: VehicleCategory, LuxuryTier, Transmission, VehicleAvailabilityStatus, VehicleMatchType, VehicleDeterminationStatus, pricingProfileSchema, vehicleSchema, VehicleAmbiguityCode/VehicleValidationErrorCode + schemas, vehicleDeterminationResultSchema",
        "packages/ai/step3: VehicleIntentService (pure, tiered exact-model/brand-only/category-only/fuzzy-typo matching against a DB-sourced lexicon), VehicleCatalogProvider seam, VehicleCatalogService (the only class touching the real fleet; alternatives widen to the general active fleet when a category-scoped search is empty), VehicleValidationService (RESOLVED/NEEDS_CLARIFICATION/UNSUPPORTED + UNKNOWN_VEHICLE/VEHICLE_INACTIVE/VEHICLE_UNAVAILABLE + confidence), VehicleDeterminationOrchestrator",
        "packages/db: Vehicle model (unique(tenantId,make,model), soft delete via deletedAt) + VehicleDetermination model + migration, vehicleRepository (create/soft-delete/lexicon/lookup/alternatives, Prisma-to-domain mapping re-validated via Zod), vehicleDeterminationRepository",
        "packages/contracts: determineVehicleParamsSchema / determineVehicleResponseSchema",
        "apps/api: PrismaVehicleCatalogProvider, POST /v1/enquiries/:conversationId/vehicle-selection (vehicleService.ts + routes/v1/vehicle.ts)",
        "docs/PHASE-3.md"
      ],
      "acceptance": [
        "every required test case passes: exact vehicle; brand only; category only; typos; unknown vehicle; malicious prompt injection (never fabricates the requested vehicle); inactive vehicle; duplicate vehicle (unique constraint, case-insensitive); tenant isolation",
        "full Phase 1 + Phase 2 regression re-run and green (no working functionality broken)",
        "all gates pass: typecheck, lint, format, 260 unit + 53 integration + 33 security tests, 4 e2e, production build — see docs/PHASE-3.md §8"
      ],
      "dependsOn": [
        2
      ],
      "phaseDoc": "docs/PHASE-3.md"
    },
    {
      "id": 4,
      "name": "Ask Missing Information (journey Step 4)",
      "status": "FROZEN",
      "goal": "Given a conversation, read whatever Steps 1-3 already resolved and verified (never raw text re-parsed) and deterministically decide what's still needed before the booking can proceed, generating one combined clarification question when something is missing.",
      "deliverables": [
        "packages/domain: RequiredField, MissingFieldReason, MissingInfoStatus, MISSING_INFO_TIMEOUT_HOURS, missingFieldSchema, collectedBookingInfoSchema, missingInfoResultSchema",
        "packages/ai/step4: RequiredFieldsEvaluator (pure, zero I/O; narrow IntentSnapshot/DateLocationSnapshot/VehicleSnapshot inputs; COMPLETE/NEEDS_INFO/EXPIRED/NOT_APPLICABLE + NOT_PROVIDED/AMBIGUOUS/INVALID reasons), clarificationPromptBuilder (deterministic, template-based, Oxford-comma joined), MissingInfoOrchestrator",
        "packages/db: MissingInfoCheck model + migration, missingInfoCheckRepository, findLatestIntentRecordForMessage added to intentRepository, findLatestVehicleDeterminationForMessage now includes the resolved vehicle relation",
        "packages/contracts: checkMissingInfoParamsSchema / checkMissingInfoResponseSchema",
        "apps/api: POST /v1/enquiries/:conversationId/missing-info (missingInfoService.ts + routes/v1/missingInfo.ts)",
        "docs/PHASE-4.md"
      ],
      "acceptance": [
        "every scenario passes: all four fields resolved (COMPLETE); a step never run (NOT_PROVIDED); an ambiguous Step 2/3 result (AMBIGUOUS); a rejected Step 2/3 result e.g. past date or inactive vehicle (INVALID); still incomplete past 24h (EXPIRED); a non-booking intent (NOT_APPLICABLE); prompt-injection flag aggregation; tenant isolation",
        "full Phase 1-3 regression re-run and green (no working functionality broken)",
        "all gates pass: typecheck, lint, format, 302 unit + 64 integration + 37 security tests, 4 e2e, production build — see docs/PHASE-4.md §8"
      ],
      "dependsOn": [
        3
      ],
      "phaseDoc": "docs/PHASE-4.md"
    },
    {
      "id": 5,
      "name": "Channels, Documents, Payments, CRM & Fulfilment",
      "status": "IN_PROGRESS",
      "goal": "Every external touchpoint behind a clean interface; full 19-step journey runnable.",
      "progressNote": "First slice: a real WhatsApp (Meta Cloud API) inbound/outbound adapter with signature verification (docs/PHASE-5-CHANNELS.md), plus runFullEnquiryPipeline, a fixed hardcoded sequence that runs Steps 1-4 automatically for one inbound message. Id 11's Conversational AI Engine (Gemini) supplies this channel's reply text instead of the fixed deterministic template alone. Second slice (2026-09-25, undocumented in a dedicated phase doc — see commit history: ffb728a, a5451df, 0e7238f, 825b671, 45839e9, 99f8284): the real Event/Workflow Engine (packages/workflow: state machine + escalation policy) plus its application layer (apps/api/src/services/journeyService.ts) now wire Steps 1-4's webhook handlers and Steps 5-8's routes into actual persisted Journey/JourneyTransition rows — the 'not persisted multi-journey resumption' gap above is closed. A parallel Email adapter (Mailgun, packages/channels/src/email) mirrors WhatsApp end to end (inbound webhook, signature verification, outbound replies), and the CRM adapter (internal default, apps/api/src/services/crmService.ts — Customer + CustomerTimelineEvent, upserted automatically from journey/webhook activity) is done. A human-escalation path (EscalationCase, SMS paging via Twilio to the tier-mapped on-call role, apps/worker's SLA-breach sweep) covers MASTER-PLAN.md's 'AI escalates to a human' requirement for Steps 1-8. Web chat, documents, payments, delivery/return, invoice PDF and the follow-up scheduler remain PENDING — none of those have a real backend yet, so no UI was built for them either (see id 12's progressNote).",
      "deliverables": [
        "Gateway inbound model; WhatsApp (Meta Cloud API + signature verify) [DONE — docs/PHASE-5-CHANNELS.md], Email (inbound webhook + outbound, Mailgun) [DONE], Web chat (REST + SSE) [PENDING]; templates, rate limits, receipts; NOT_CONFIGURED per adapter",
        "Event/Workflow Engine: persisted Journey/JourneyTransition state machine (packages/workflow) wired into Steps 1-8's real routes/webhooks [DONE]; human escalation (EscalationCase, tier-mapped SMS paging via Twilio, SLA-breach sweep) [DONE]",
        "Document pipeline: presigned S3 uploads, MIME/size validation, AV-scan interface, extraction interface, checklist, T2 review queue [PENDING]",
        "PaymentProvider interface + instructions + webhook reconciliation + refunds; NOT_CONFIGURED -> explicit manual path flagged in admin [PENDING]",
        "CRM adapter (internal default) [DONE], delivery/return coordination (slots, assignment, checklists, photos), invoice PDF, follow-up scheduler [PENDING]"
      ],
      "acceptance": [
        "full journey 01-19 passes e2e with test doubles inside tests only",
        "production wiring shows real provider status per adapter in admin Settings",
        "webhook signature + replay tests [DONE for WhatsApp — docs/PHASE-5-CHANNELS.md §7-8]"
      ],
      "dependsOn": [
        4
      ],
      "phaseDoc": "docs/PHASE-5-CHANNELS.md"
    },
    {
      "id": 6,
      "name": "Eligibility (journey Step 5 — ELIGIBILITY_CHECK)",
      "status": "FROZEN",
      "goal": "Given validated customer data, the vehicle/dates/location Steps 2-3 already resolved, and driver requirements, deterministically decide ELIGIBLE / INELIGIBLE / NEEDS_HUMAN_REVIEW against a tenant-configurable policy — MASTER-PLAN.md journey Step 5 (`ELIGIBILITY_CHECK`, \"SYS\" owner).",
      "deliverables": [
        "packages/domain/src/eligibility.ts: LicenseType; EligibilityRuleCategory/Outcome; EligibilityExceptionType/RiskLevel; driverInputSchema/eligibilityCustomerInputSchema (new request-boundary input — Steps 1-4 never collect age/license/passport/nationality); EligibilityPolicyRules/EligibilityPolicy (versioned, configurable); EligibilityException; PolicyConflictCode; EligibilityDecisionStatus/Result",
        "packages/ai/src/step5: 7 deterministic EligibilityRule implementations (AGE/LICENSE/PASSPORT/NATIONALITY/VEHICLE/LOCATION/DRIVER_REQUIREMENT), policyValidator (fail-safe policy-conflict detection → NEEDS_HUMAN_REVIEW), exceptionResolver (LOW-risk auto-waives a rule, HIGH-risk always escalates to a human), reasonBuilder (deterministic, template-based, never AI-generated), EligibilityOrchestrator — zero AI/LLM calls anywhere in this step",
        "packages/db: EligibilityPolicy (tenant-scoped, versioned, append-only)/EligibilityException/EligibilityDecision Prisma models + additive migration, eligibilityPolicyRepository/eligibilityExceptionRepository/eligibilityDecisionRepository, a default policy seeded for the demo tenant",
        "packages/contracts/src/eligibility.ts: checkEligibilityParamsSchema / checkEligibilityBodySchema (`.strict()`) / checkEligibilityResponseSchema",
        "apps/api: POST /v1/enquiries/:conversationId/eligibility (eligibilityService.ts + routes/v1/eligibility.ts) — every decision persisted + an audit event written in the same transaction",
        "docs/PHASE-11.md"
      ],
      "acceptance": [
        "every required test case passes: valid customer; underage; missing license; invalid license; nationality exception; VIP exception; policy conflict; tampered request; tenant isolation",
        "full Phase 1-4 regression re-run and green (no working functionality broken)",
        "every decision is auditable and no AI call is ever in the decision path",
        "all gates pass: typecheck, lint, format, unit + integration + security tests, 4 e2e, production build — see docs/PHASE-11.md §8"
      ],
      "dependsOn": [
        4
      ],
      "phaseDoc": "docs/PHASE-11.md"
    },
    {
      "id": 7,
      "name": "Availability (journey Step 6 — AVAILABILITY_CHECK)",
      "status": "FROZEN",
      "goal": "Real, database-backed inventory availability: given a conversation's already-resolved Step 3 vehicle and Step 2 dates, authoritatively decide AVAILABLE/HELD/BOOKED/UNAVAILABLE/MAINTENANCE/UNKNOWN and place a TTL'd reservation lock — never a guess, never told to a customer unless the lock-protected claim itself confirms it.",
      "deliverables": [
        "packages/domain: InventoryStatus, HoldStatus, AvailabilityErrorCode, holdSchema, availabilityCheckResultSchema",
        "packages/ai/step6: FleetProvider + FleetProviderError seam, ResilientFleetProvider (timeout/circuit-breaker/rate-limit), computeInventoryStatus + rangesOverlapWithBuffer (pure), AvailabilityProvider seam, AvailabilityCheckOrchestrator + validateAvailabilityRequest",
        "packages/db: VehicleUnit model (real countable inventory) + AvailabilityHold model (capacity-based TTL'd/confirmed reservation lock, unique idempotencyKey, optimistic version) + AvailabilityCheck model (append-only history) + migration; vehicleUnitRepository, availabilityHoldRepository (pg_advisory_xact_lock via acquireVehicleLock, buffered+lazily-expiring countOverlappingHolds, optimistic updateHoldStatus, expireDueHolds sweep), availabilityCheckRepository",
        "apps/api: DatabaseFleetProvider (default, real) / ExternalFleetApiProvider / NotConfiguredFleetProvider / CachedFleetProvider (Redis, external path only), PrismaAvailabilityProvider, ReservationLockService (placeHold pessimistic-locked + idempotent + self-validating; releaseHold/confirmHold optimistic + audited), shared evaluateInventoryStatus, POST /v1/enquiries/:conversationId/availability-check",
        "apps/worker: startHoldExpirationSweep (plain interval, housekeeping only — correctness never depends on it)",
        "docs/PHASE-12.md"
      ],
      "acceptance": [
        "every required test case passes: concurrent booking (N units, N+1 concurrent requests); double booking; expired hold (lazy expiration, including a confirmHold-on-lapsed-hold regression); API timeout; provider failure; duplicate request (idempotency, incl. a concurrent-duplicate race); race condition (10-way, single unit); tenant isolation",
        "full Phase 1-4 regression re-run and green (no working functionality broken)",
        "all gates pass: typecheck, lint, format, unit + integration + security tests, 4 e2e, production build — see docs/PHASE-12.md §5"
      ],
      "dependsOn": [
        4
      ],
      "phaseDoc": "docs/PHASE-12.md"
    },
    {
      "id": 8,
      "name": "Alternatives (journey Step 7 — OFFERING_ALTERNATIVES)",
      "status": "FROZEN",
      "goal": "Given a conversation's already-resolved Step 3 vehicle and Step 2 dates, deterministically rank real, currently-available alternative vehicles — MASTER-PLAN.md journey Step 7 (`OFFERING_ALTERNATIVES`, \"AI→SYS\" owner: AI may explain, never decide).",
      "deliverables": [
        "packages/domain/src/alternatives.ts: AlternativeRecommendationStatus, AlternativesErrorCode, alternativeCandidateSchema, recommendAlternativesResultSchema",
        "packages/ai/src/step7: rankAlternatives (pure, deterministic six-stage ladder: availability filter -> customer-constraint filter -> category/luxury-tier/price/preference scoring), buildAlternativeReason (deterministic, template-based, never AI-generated), AlternativeRecommendationOrchestrator — zero AI/LLM calls anywhere in this step, reuses Step 3's VehicleCatalogProvider (candidate generation) and Step 6's AvailabilityProvider (live per-candidate availability preview, never placeHold)",
        "packages/ai/src/step3/vehicleCatalogProvider.ts: findAlternativesWithFallback extracted and shared with vehicleCatalogService.ts",
        "packages/db: AlternativeRecommendation model (append-only) + migration, alternativeRecommendationRepository (create + findLatest, id-tiebreak for deterministic reads)",
        "packages/contracts/src/alternatives.ts: recommendAlternativesParamsSchema / recommendAlternativesResponseSchema",
        "apps/api: alternativesService.ts + routes/v1/alternatives.ts, POST /v1/enquiries/:conversationId/alternatives — never places a hold (human decision remains available)",
        "docs/PHASE-13.md"
      ],
      "acceptance": [
        "every required test case passes: no alternative; one alternative; many alternatives; same-category alternatives; price conflict; availability conflict; prompt injection; biased recommendation test",
        "full regression re-run and green (no working functionality broken)",
        "never recommends inventory the live AvailabilityProvider check did not confirm AVAILABLE at ranking time; price ranking proven free of upsell bias",
        "all gates pass: typecheck, lint, format, 541 unit + 205 integration + 79 security tests, 4 e2e, production build — see docs/PHASE-13.md §8"
      ],
      "dependsOn": [
        7
      ],
      "phaseDoc": "docs/PHASE-13.md"
    },
    {
      "id": 9,
      "name": "Quote / Pricing Engine (journey Step 8 — QUOTE_ISSUED)",
      "status": "FROZEN",
      "goal": "Given a conversation's already-resolved Step 3 vehicle and Step 2 dates, deterministically compute a complete, immutable, versioned, tamper-evident price quote — MASTER-PLAN.md journey Step 8 (`QUOTE_ISSUED`, \"SYS\" owner: base rate, duration tiers, extras, deposit, VAT 5%, quote expiry).",
      "deliverables": [
        "packages/domain/src/money.ts: Money (integer-minor-unit value object — fils, never floating-point arithmetic for financial totals), moneySchema (.nonnegative()), sumMoney",
        "packages/domain/src/quote.ts: InsuranceTier, quoteSelectionsSchema (`.strict()`), QuoteLineItemCategory, quoteLineItemSchema/quoteTaxLineSchema/quoteFeeLineSchema/quoteDiscountLineSchema, QuoteStatus, QuoteErrorCode, quoteSnapshotSchema",
        "packages/ai/src/step8: PricingRules + DEFAULT_PRICING_RULES (base rate/weekly-tier blending, extras, delivery, insurance, VAT, service fee, discount codes, default deposit, validity window, human-review threshold), calculatePricing (pure, deterministic, zero AI/LLM calls), detectPricingAnomalies (flags PENDING_REVIEW for a large discount, a seam-only manual override, or a zero total), QuoteValidator (HMAC-SHA256 tamper detection over the quote's own priced content, then expiry)",
        "packages/db: QuoteStatus enum + Quote model (append-only, @@unique([tenantId, quoteId, version])) + relations + additive migrations, quoteRepository (acquireConversationQuoteLock via pg_advisory_xact_lock, createQuote, findLatestQuoteForConversation, toDomainQuoteSnapshot)",
        "packages/contracts/src/quote.ts: request/response schemas for both endpoints",
        "apps/api: quoteService.ts (createQuote re-validates Steps 2-3 staleness, tamper-aware duplicate-quote reuse, audited) + routes/v1/quote.ts, POST + GET /v1/enquiries/:conversationId/quote — customer/driver data never accepted as a money-bearing input",
        "docs/PHASE-14.md"
      ],
      "acceptance": [
        "every required test case passes: 4 days; fees; tax; discount; rounding; zero/negative values; currency mismatch; expired quote; duplicate quote; concurrent quote generation",
        "full regression re-run and green (no working functionality broken)",
        "security: price manipulation prevention, authorization, audit trail, and tamper detection each independently proven; human review: a large discount produces PENDING_REVIEW + reviewReasons",
        "all gates pass: typecheck, lint, format, 599 unit + 232 integration + 91 security tests, 4 e2e, production build — see docs/PHASE-14.md §8"
      ],
      "dependsOn": [
        4
      ],
      "phaseDoc": "docs/PHASE-14.md"
    },
    {
      "id": 10,
      "name": "Security Engine & Zero Trust",
      "status": "FROZEN",
      "goal": "Zero-trust layering verified by tests (WAF -> AuthN -> AuthZ -> Tenant -> Sandbox -> Egress -> DB policy -> Encrypted data).",
      "deliverables": [
        "Edge: Redis-backed rate limiting (shared across instances) + a stricter dedicated limit on /v1/auth/*; known-scanner-user-agent WAF denylist",
        "AuthN: real, working local email+password+TOTP-MFA staff auth (argon2id, <=15-min HS256 access tokens, rotating opaque refresh tokens with reuse detection incl. the concurrent-refresh race); IdentityProvider OIDC seam + NotConfiguredIdentityProvider — see docs/SECURITY-MODEL.md §3",
        "AuthZ: RBAC role->permission matrix + ABAC tenant-match policy engine (authorize()), enforced as Fastify preHandlers (authenticate, requirePermission) on every admin route",
        "Tenant isolation: Postgres RLS (ENABLE+FORCE ROW LEVEL SECURITY) on every tenant-scoped table, keyed on a per-transaction app.tenant_id session setting (withTenantContext()); real least-privilege ai_concierge_api/ai_concierge_worker DB roles (no DELETE/DDL/BYPASSRLS); cross-tenant negative tests proven against the real scoped role connection",
        "AI sandbox: tool permission matrix seam (packages/ai/src/sandbox); egress allowlist unchanged from Phase 1 (ssrfSafeFetch, now also used by the Gemini adapter — id 11); injection defences unchanged from Phase 1-2; output DLP tripwire on outbound text — id 11's Gemini-generated replies are the first real free-text AI output this tripwire actually screens",
        "Data: TLS at the edge; AES-256-GCM field-level encryption applied to the MFA TOTP seed; docs/SECURITY-MODEL.md's key rotation runbook",
        "Detection & response: SecurityEvent append-only stream + five anomaly/detection rules with automatic response (session revocation via Redis family-revocation set) and manual response (POST /v1/users/:id/lock|unlock); GET /v1/security-events as the current T4 escalation surface",
        "CI: pnpm audit + gitleaks + Semgrep; Trivy image scan and ZAP baseline explicitly deferred to the Infrastructure phase (no Dockerfile/deployed ephemeral environment exist yet)",
        "docs/SECURITY-MODEL.md (full STRIDE table, zero-trust layer mapping, RBAC matrix, key rotation runbook) + apps/api/src/killChain.security.test.ts"
      ],
      "acceptance": [
        "kill-chain test proves each layer contains a compromised account — AuthZ containment, tenant-isolation containment (RLS), and token-theft containment",
        "authz matrix tests — full role x permission matrix incl. the 'ADMIN is always a superset' invariant",
        "all scanners green with zero critical — pnpm audit/gitleaks/Semgrep green; Trivy/ZAP deferred with a documented reason, not silently skipped"
      ],
      "dependsOn": [
        4
      ],
      "phaseDoc": "docs/PHASE-6.md"
    },
    {
      "id": 11,
      "name": "Conversational AI Engine (Gemini)",
      "status": "FROZEN",
      "goal": "Give the WhatsApp channel a real, human-like conversational layer without weakening any existing guarantee: Steps 1-4 stay 100% deterministic and remain the only source of booking facts; a Gemini-backed AIProvider only phrases the reply around whatever those steps already verified, strictly grounded, with a deterministic-template fallback on any failure. Also fixed the conversation-memory gap the WhatsApp channel had (every inbound message starting an unrelated fresh conversation) — since merged with id 5's own independently-built, more complete conversation-continuity work (findOpenConversationForCustomer/continueEnquiry, short-reply and booking-continuation detection).",
      "deliverables": [
        "packages/ai: AIProvider interface evolved to a real structured-generation + health-check contract (systemInstruction/prompt/responseSchema in, unknown JSON + usage + modelId out — callers must Zod-validate, never trust AI output); ResilientAIProvider decorator (timeout + circuit breaker + rate limit), same pattern as Step 2's ResilientLocationProvider",
        "apps/api/src/lib/geminiProvider.ts: GeminiProvider (direct REST call via ssrfSafeFetch against a single hardcoded host, API key sent via header never URL/logs) + createAIProvider factory (GEMINI_API_KEY alone gates CONFIGURED/NOT_CONFIGURED; GEMINI_MODEL_ID/TEMPERATURE/MAX_OUTPUT_TOKENS/TIMEOUT_MS configurable, safe defaults)",
        "apps/api/src/services/conversationalReplyService.ts: generateConversationalReply — grounded strictly in Step 4's verified result and recent turns, schema-validated, a second code-level grounding check (rejects invented prices/availability/booking-confirmation language even from well-formed JSON), falls back to the deterministic template (buildWhatsAppReplyText, now in @ai-concierge/channels) on NOT_CONFIGURED / schema-invalid / grounding-violation / provider-error / open-circuit",
        "apps/api/src/routes/webhooks/whatsapp.ts wired to the new reply service (on top of id 5's runFullEnquiryPipeline + claim-based idempotency); apps/api/src/server.ts boot-time Gemini health check so a bad model id/unreachable API surfaces in logs immediately instead of silently degrading",
        "New AppError code AI_RESPONSE_INVALID (502) for a provider response that's empty, safety-blocked, malformed, or fails validation",
        ".env.example / render.yaml updated with GEMINI_* vars; docs/MASTER-PLAN.md §9 decision #8 updated (Gemini adapter behind the existing AIProvider interface; Anthropic/OpenAI remain unbuilt future implementations of the same interface)",
        "docs/phases/PHASE-06.md; merge reconciliation with id 5/id 10's independently-built conversation-continuity and Security Engine work (2026-09-25)"
      ],
      "acceptance": [
        "every required scenario passes: provider NOT_CONFIGURED falls back to the exact pre-existing deterministic reply; a well-formed grounded AI reply is used; a schema-invalid AI response falls back; a reply inventing a price or claiming availability/booking-confirmation is rejected by the grounding check and falls back even though the JSON was well-formed; a prompt-injection attempt in a customer turn is sanitized before it reaches the model prompt and cannot force a fabricated price",
        "full regression re-run and green (no working functionality broken) — verified against real Postgres/Redis in this sandbox (no Docker daemon here, same documented limitation as every prior phase)",
        "all gates pass: typecheck, lint, format, unit + integration + security tests, 4 e2e, production build, /code-review findings addressed — see docs/phases/PHASE-06.md §8"
      ],
      "dependsOn": [
        5
      ],
      "phaseDoc": "docs/phases/PHASE-06.md"
    },
    {
      "id": 12,
      "name": "Admin Dashboard (Web)",
      "status": "IN_PROGRESS",
      "goal": "Dashboard wireframe, pixel-faithful to the design system.",
      "progressNote": "Scoped deliberately to Steps 1-8 (this system's real, tested backend) per explicit request: a working staff dashboard now exists (apps/web/src/app/dashboard) with cookie-based auth (httpOnly access+refresh tokens, silent keep-alive refresh, `packages/db/src/bootstrapAdmin.ts` for the first account), a shared TopBar+nav shell, and five real screens — Escalation Queue (list/filter/assign/resolve, wired to the real EscalationCase workflow), Journeys (list + detail with full transition timeline), Customers (CRM list + detail with timeline), Fleet (read-only catalog), and Settings (live per-provider CONFIGURED/NOT_CONFIGURED status, never fake). All screens are Server Components reading real API data server-side; mutations are Server Actions. Deliberately NOT built: Bookings, Document review, Pricing, Payments/Invoices and Audit log screens — none of those have real backend functionality yet (id 5's Documents/Payments/delivery remain PENDING), so building UI for them would be exactly the fake-functionality CLAUDE.md forbids. Also not built: the Home screen's live JourneyStepper/StatTiles (no aggregate-count endpoint exists yet — Home currently links to each section instead of showing fabricated numbers), SSE real-time updates, per-role nav hiding (every staff role already has the same journey/escalation/customer read+act permissions — see packages/domain/src/auth.ts's own doc on this), and Playwright e2e/visual-regression coverage. Verified by hand against a real Postgres/Redis dev stack and a real browser (login -> every screen -> full escalation assign/resolve cycle -> sign-out -> auth-gate redirect), not just typecheck/lint/unit.",
      "deliverables": [
        "Home: TopBar, nav [DONE]; JourneyStepper (live counts), StatTiles (Active Bookings, AI Automation %, Human Escalations, Revenue AED) [PENDING — no aggregate-count endpoint yet]",
        "Journeys (live state + timeline) [DONE], Escalation queue (tiers, SLA, assign/resolve) [DONE], Customers [DONE], Fleet (read-only) [DONE], Settings (provider status) [DONE]",
        "Bookings, Document review, Fleet availability editing, Pricing, Payments/Invoices, Audit log [PENDING — no real backend yet]",
        "Real-time via SSE [PENDING], role-aware navigation [N/A — every staff role shares the same dashboard permissions by design]",
        "Playwright e2e + visual regression against reference [PENDING]"
      ],
      "acceptance": [
        "screens match DESIGN-SYSTEM.md contracts — done for the 5 screens built (Emerald&Copper tokens, GlassCard/CopperCard/PillButton/StatusChip)",
        "visual regression green against docs/design/reference [PENDING — no Playwright visual-regression suite yet]",
        "role-aware nav enforced [N/A — see progressNote]"
      ],
      "dependsOn": [
        10
      ],
      "phaseDoc": "docs/phases/PHASE-07.md"
    },
    {
      "id": 13,
      "name": "Customer Mobile App (PWA)",
      "status": "PENDING",
      "goal": "Mobile wireframe + reference image, pixel-faithful.",
      "deliverables": [
        "Home: Monogram, AI CONCIERGE, Upcoming Rental CopperCard, Documents GlassCard, Payment CopperCard, CONTACT AI",
        "Chat with AI (web channel), Bookings, Document upload with camera, Payments, Profile, BottomTabBar",
        "Installable PWA, offline shell, push-notification interface (NOT_CONFIGURED until keys)",
        "Visual regression against docs/design/reference"
      ],
      "acceptance": [
        "home matches the reference mockup",
        "installable PWA + offline shell",
        "visual regression green"
      ],
      "dependsOn": [
        12
      ],
      "phaseDoc": "docs/phases/PHASE-08.md"
    },
    {
      "id": 14,
      "name": "Observability, AI Evaluation & Automatic QA",
      "status": "PENDING",
      "goal": "Measurable, self-checking system; Automatic QA PASS/FAIL gate.",
      "deliverables": [
        "OTel end-to-end (api, worker, web) with journey-id propagation",
        "Metrics: automation rate, escalation rate, step conversion, latency, AI cost; Grafana dashboards in compose",
        "AI evaluation suite (accuracy, safety, hallucination, tone)",
        "Security regression suite + anomaly engine",
        "Automatic QA engine: PASS/FAIL per journey and per release + CI report artifact"
      ],
      "acceptance": [
        "traces span api->worker->web with journey id",
        "Automatic QA gate blocks release on FAIL",
        "eval + security regression thresholds enforced in CI"
      ],
      "dependsOn": [
        13
      ],
      "phaseDoc": "docs/phases/PHASE-09.md"
    },
    {
      "id": 15,
      "name": "Infrastructure, CI/CD & Release",
      "status": "PENDING",
      "goal": "Production-ready delivery.",
      "deliverables": [
        "Terraform (network, Postgres, Redis, object storage, container runtime, secrets, WAF)",
        "dev/staging/prod environments",
        "CI/CD with mandatory gates + manual prod approval",
        "migration strategy, backups/DR drill, runbooks, SLOs/alerts, release-freeze checklist"
      ],
      "acceptance": [
        "terraform plan/apply on staging",
        "prod deploy gated by all checks + manual approval",
        "DR drill documented and rehearsed"
      ],
      "dependsOn": [
        14
      ],
      "phaseDoc": "docs/phases/PHASE-10.md"
    },
    {
      "id": 16,
      "name": "Automatic Steps 5-8 chaining + Conversation Engine v2",
      "status": "IN_PROGRESS",
      "goal": "A customer message advances the journey by itself from Step 4 to an issued quote (or alternatives / a human hand-off), and every reply is Gemini-worded but grounded in deterministic facts.",
      "progressNote": "Implemented and green on typecheck, lint, unit and the integration/security suites listed in docs/PHASE-16.md: driver-detail intake over chat (deterministic + evidence-grounded Gemini extraction, DOB encrypted at rest), the Step 5->6->8/7 chain reusing each step service and record*Outcome, human hand-off for requested person / complaint / accepted quote / expired quote / step failure / stalled details, a draft-then-rewrite reply engine with a grounding guard, stored outbound replies, one shared handleInboundTurn for WhatsApp and Email, journey-aware open-conversation lookup. NOT frozen: Playwright e2e and an independent code review were not run. Steps 9-19, the Customer PWA and an email retry feature remain PENDING.",
      "deliverables": [
        "EligibilityIntake + OutboundMessage tables (RLS forced) and repositories [DONE]",
        "Intake extraction: deterministic + Gemini with verbatim-evidence grounding [DONE]",
        "journeyAutopilotService: automatic Step 5 -> 6 -> 8 / 7 with human hand-off rules [DONE]",
        "journeyReplyService: grounded draft + Gemini rewrite, grounding guard, deterministic fallback [DONE]",
        "Shared handleInboundTurn for WhatsApp and Email [DONE]",
        "Playwright e2e + independent code review [PENDING]"
      ],
      "acceptance": [
        "A first message with vehicle/dates followed by the driver details yields an issued quote with no manual endpoint call",
        "Ambiguous or missing input is asked again, never guessed",
        "A reply that changes or invents a number/claim never reaches the customer",
        "Accepting a quote or asking for a person creates exactly one EscalationCase"
      ],
      "dependsOn": [
        4,
        5,
        6,
        7,
        8,
        9,
        10,
        11,
        12
      ],
      "phaseDoc": "docs/PHASE-16.md"
    }
  ]
}
