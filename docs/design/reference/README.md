# Reference images

The UI/UX of the app **must** match these references (see `docs/DESIGN-SYSTEM.md`).
Visual regression tests in Phases 7 and 8 compare rendered screens against them.

Expected files (add the actual images here):

| File | What it shows |
|---|---|
| `mobile-home.jpg` | Two-phone mockup: emerald background, brushed-copper cards, cream identity card, "GLOBAL CONNECT" wordmark + "N" monogram. The customer-app home target. |
| `dashboard-home.png` | Admin dashboard home wireframe (TopBar, JourneyStepper, StatTiles). |

Keep the palette and layout as defined in `DESIGN-SYSTEM.md`:
deep emerald green + brushed rose-gold/copper, thin uppercase headings with wide tracking,
rounded cards, pill controls.
